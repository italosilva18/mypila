# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/m2m-financeiro

# JWT Configuration
# CRITICAL: Generate a strong random secret for production!
# You can generate one with: openssl rand -base64 32
# Or use the one generated on first run (check server logs)
JWT_SECRET=your_secure_random_jwt_secret_here_minimum_32_characters

# Server Configuration
PORT=8080

# SECURITY WARNING:
# - NEVER commit the actual .env file with real secrets to version control
# - NEVER use "default_secret_key_change_me" in production
# - Rotate JWT_SECRET periodically in production
# - Use different secrets for development, staging, and production environments
